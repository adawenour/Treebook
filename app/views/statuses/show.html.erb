

</p>

<p>
  <strong>Content:</strong>
  <%= @status.content %>
</p>
<% if @status.user == current_user %>
<%= link_to 'Edit', edit_status_path(@status) %> |
<% end %>
<%= link_to 'Back', statuses_path %>

<h3>Comments</h3>
<% @statuses.comments.each do |comment| %>
  <div>
    <strong> comment by <%= @statuses.user.profile_name %> </strong>
    <br />
    <p><%= comment.body %></p>
  </div>
<% end %>

	<%= render 'comments/form' %>